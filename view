<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Inventory App</title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <header>
    <img src="/public/img.jpg" alt="aaaaaaaaaaaa">
    <h1>Inventory Management</h1>
  </header>

  <main>
    <section class="form-section">
      <form action="/add" method="POST" class="form">
        <input type="text" name="name" placeholder="Item name" required />
        <input type="number" name="quantity" placeholder="Quantity" required />
        <input type="number" step="0.01" name="price" placeholder="Price" required />
        <button type="submit">Add Item</button>
      </form>
    </section>

    <section class="table-section">
      <table>
        <thead>
          <tr>
            <th>Item</th>
            <th>Qty</th>
            <th>Price</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% items.forEach(item => { %>
            <tr>
              <td><%= item.name %></td>
              <td><%= item.quantity %></td>
              <td>$<%= item.price %></td>
              <td>
                <form action="/edit/<%= item._id %>" method="POST" class="inline-form">
                  <input type="text" name="name" value="<%= item.name %>" required />
                  <input type="number" name="quantity" value="<%= item.quantity %>" required />
                  <input type="number" step="0.01" name="price" value="<%= item.price %>" required />
                  <button class="edit-btn">💾</button>
                </form>
                <form action="/delete/<%= item._id %>" method="POST" class="inline-form">
                  <button class="delete-btn" onclick="return confirm('Delete this item?')">🗑️</button>
                </form>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </section>
  </main>

  <footer>
    <p>© 2025 Inventory App</p>
  </footer>
</body>
</html>

